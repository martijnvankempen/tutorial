# tutorial
## Install Docker
```https://docs.docker.com/install/```

## Checkout code
```git clone ... ```

## Build and run
```docker-compose build --force-rm && docker-compose up -d --force-recreate```